using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlourLayout : MonoBehaviour
{
    public GameObject eyeCorn;
    GetTarget getTarget;

    // Start is called before the first frame update
    void Start()
    {
        // Cone = GameObject.Find("Cone");
        // GetTarget = Cone.GetComponent();
        // getTarget = eyeCorn.GetComponent<GetTarget>();

        // int number = GetTarget.TargetCount;

        // リストを数える
        // int ObjCount = getTarget.myList.Count;

        // for (int i = 0; i <= ObjCount; i++){
        //     Debug.Log(ObjCount);

        // GameObject _target = Instantiate(GetRandomTemplate());
        // ここをリストからとるようにする



        // }


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
